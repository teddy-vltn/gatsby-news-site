image: node:latest

cache:
  paths:
    - .cache/
    - node_modules/
    - public/

pages:
  script:
    - yarn install
    - ./node_modules/.bin/gatsby build --prefix-paths
  artifacts:
    paths:
      - public
  only:
    - main